export type Location = { lng: number; lat: number };

export const DEFAULT_CENTER: Location = { lng: 0, lat: 0 };
export const DEFAULT_ZOOM = 2;
export const DEFAULT_BEARING = 0;
export const DEFAULT_PITCH = 0;
